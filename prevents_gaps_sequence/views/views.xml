<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="prevents_gaps_sequence.list">
      <field name="name">prevents_gaps_sequence list</field>
      <field name="model">prevents_gaps_sequence.prevents_gaps_sequence</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="prevents_gaps_sequence.action_window">
      <field name="name">prevents_gaps_sequence window</field>
      <field name="res_model">prevents_gaps_sequence.prevents_gaps_sequence</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="prevents_gaps_sequence.action_server">
      <field name="name">prevents_gaps_sequence server</field>
      <field name="model_id" ref="model_prevents_gaps_sequence_prevents_gaps_sequence"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="prevents_gaps_sequence" id="prevents_gaps_sequence.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="prevents_gaps_sequence.menu_1" parent="prevents_gaps_sequence.menu_root"/>
    <menuitem name="Menu 2" id="prevents_gaps_sequence.menu_2" parent="prevents_gaps_sequence.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="prevents_gaps_sequence.menu_1_list" parent="prevents_gaps_sequence.menu_1"
              action="prevents_gaps_sequence.action_window"/>
    <menuitem name="Server to list" id="prevents_gaps_sequence" parent="prevents_gaps_sequence.menu_2"
              action="prevents_gaps_sequence.action_server"/>
-->
  </data>
</odoo>
